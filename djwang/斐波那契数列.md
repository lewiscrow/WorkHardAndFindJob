```
	public int fib(int n) {
        if(n==0) return 0;
        if(n==1) return 1;
        //递归超时，改用动态规划，时间复杂度由O(n^2)变为O(n),空间复杂度为O(n)
        int[] fibs=new int[n+1];
        fibs[1]=1;
        for(int i=2;i<=n;i++){
            fibs[i]=(fibs[i-1]+fibs[i-2])%1000000007;
        }
        return fibs[n];
    }
```