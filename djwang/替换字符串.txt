
遍历字符串，替换空格，复杂度都为O(n)
    public String replaceSpace(String s) {
        String a="";
        for(int i=0;i<s.length();i++){
            if(s.charAt(i)==' ') a+="%20";
            else a+=s.charAt(i);
        }
        return a;
        //return s.replaceAll(" ","%20");
    }
